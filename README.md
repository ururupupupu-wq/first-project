# Начало работы с Git

## 📅 Даты модуля
- **Начало:** 12/12  
- **Окончание:** 12/28  

---

## 📚 Содержание

### 🔧 Основы Git
- Инициализируем репозиторий
- Добавляем файлы в репозиторий
- Делаем первый коммит
- Просматриваем историю коммитов

---

### 🌍 Знакомство с GitHub
- Регистрация на GitHub
- Создаём удалённый репозиторий
- Что такое SSH
- Генерируем SSH-ключ
- Привязываем SSH-ключ к GitHub
- Связываем локальный и удалённый репозитории
- Синхронизируем локальный и удалённый репозитории

---

### 📄 README.md
- Назначение файла `README.md`
- Практическая работа 1: **Делимся проектом с миром**

---

### 🧠 Коммиты и история изменений
- Хеш — идентификатор коммита
- Исследуем лог
- `HEAD` — всему голова
- Статусы файлов в Git
- Как читать `git status`
- Оформление сообщений к коммитам
- Практическая работа 2: **Дополняем шпаргалку**

---

### 🔄 Исправления и откаты
- Как исправить коммит
- Как откатиться назад, если «всё сломалось»
- Практическая работа 3: **Откатываем изменения**

---

### 🔍 Анализ изменений
- Просматриваем изменения в файлах
- Сопоставляем коммиты
- Игнорирование файлов в Git
- Практическая работа 4: **Исследуем историю коммитов**

---

## 📝 Шпаргалка

### Ключевые команды модуля
- Прогресс: **0 / 16**

### Практика
- Прогресс: **0 / 2**

---

## 🧑‍💻 Регистрация на GitHub

Вы познакомились с платформой **GitHub** — пришло время зарегистрироваться на ней. Поехали!

### 📌 Инструкция по регистрации

1. В правом верхнем углу главной страницы GitHub нажмите **Sign up**.
2. На экране будут последовательно появляться поля для ввода:
   1. Введите адрес электронной почты  
      (*Enter your email*).
   2. Придумайте пароль  
      (*Create a password*).
   3. Введите имя пользователя  
      (*Enter a username*).
3. Платформа спросит, хотите ли вы получать рассылку с обновлениями и новостями:
   - Введите `y`, если хотите получать рассылку.
   - Введите `n`, если не хотите.
4. Нажмите кнопку **Continue**.
5. GitHub предложит пройти капчу — сделайте это.
6. После прохождения капчи нажмите **Create account**.
7. Введите короткий код, который будет отправлен на указанный вами почтовый адрес.

---

## 🔑 Что такое хеш. Хеширование коммитов

**Хеширование** (от англ. *hash* — «рубить», «крошить», «мешанина») — это способ
преобразовать набор данных и получить их **«отпечаток»** (*fingerprint*).

Информация о коммите — это набор данных: когда был сделан коммит, содержимое
файлов в репозитории на момент коммита и ссылка на предыдущий (родительский,
*parent*) коммит.

Git хеширует (преобразует) информацию о коммите с помощью алгоритма **SHA-1**
(*Secure Hash Algorithm — «безопасный алгоритм хеширования»*) и получает для
каждого коммита **уникальный хеш** — результат хеширования.

Обычно хеш — это короткая строка (**40 символов** в случае SHA-1), которая
состоит из цифр `0–9` и латинских букв `A–F` (регистр не имеет значения). 
Пример хеша:  
`e83c5163316f89bfbde7d9ab23ca2e25604af290`.

Хеш обладает важными свойствами: если получить хеш **дважды для одного и
того же набора данных**, результат будет **одинаковым**; если изменить
**хотя бы один символ** во входных данных, хеш **сильно изменится**. 

Чтобы убедиться в этом, можно поэкспериментировать с SHA-1 на специальных
сайтах: введите данные в поле **input** и посмотрите, как меняется хеш в
поле **output**.

Git хранит таблицу соответствий: `хеш → информация о коммите`. Если вы знаете
хеш, вы можете узнать автора коммита, дату коммита и содержимое
закоммиченных файлов. Поэтому **хеш — основной идентификатор коммита**.

При работе с Git хеши встречаются регулярно: их можно передавать как параметры
Git-командам и указывать, с каким именно коммитом нужно выполнить действие.

Все хеши и таблицу соответствий Git хранит в служебных файлах внутри скрытой
папки `.git/`, которая находится в корне репозитория проекта.

## 📜 Элементы описания коммита и сокращённый лог

Разберём элементы, из которых состоит описание коммита: строка из цифр и латинских букв
после слова `commit` — это **хеш коммита**; `Author` — имя автора и его электронная почта;
`Date` — дата и время создания коммита; в конце находится **сообщение коммита**.

Получить сокращённый лог можно с помощью команды `git log --oneline`. В терминале
появятся только первые несколько символов хеша каждого коммита и их комментарии.

Сокращённый лог особенно полезен, если в репозитории уже много коммитов —
например, сотни или тысячи. В этом случае можно быстро найти нужный коммит
по описанию. Сокращённый хеш (первые несколько символов полного) можно использовать
точно так же, как и полный. Команда `git log --oneline` автоматически подбирает
длину сокращённых хешей, чтобы они были уникальными в пределах репозитория и Git
всегда мог понять, о каком коммите идёт речь.

